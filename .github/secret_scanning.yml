paths-ignore:
  # Firebase client config contains a public API key (project identifier, not a secret).
  # It is designed to be embedded in client-side code per Firebase docs.
  - 'config.prod.js'
  - 'config.example.js'
