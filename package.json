{
  "name": "geotools",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "GeoTools — circle triangulation + meeting point finder",
  "scripts": {
    "lint": "eslint app.js auth.js config.prod.js",
    "test": "npm run lint"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "globals": "^16.0.0"
  }
}
